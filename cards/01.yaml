question: |-
  Define a new variable `color` that
    - equals the string "green" if the variable `value` is a positive integer
    - and otherwise equals the string "red"
answer: |-
  color = "green" if value > 0 else "red"
